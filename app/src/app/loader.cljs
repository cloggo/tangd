(ns app.loader
  (:require [app.controller.rec]
            [app.controller.adv]
            [app.controller.rotate-keys]
            [app.controller.t-error]
            [app.service.schema]
            [restify.restify]))
